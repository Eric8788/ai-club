# AI Club Hub 升级需求与数据库方案汇总 (HUB_UPGRADE_SPEC)

这份文档旨在为后续的 AI 助手（如 Codex）提供 Hub 升级的详细技术指令。

## 1. 项目目标
将当前的静态 `index.html` 升级为数据驱动的架构（推荐 **Vite + React**），并引入数据库（前期可使用 `projects.json`）管理内容。

## 2. 视觉风格要求 (必须完美保留)
*   **配色方案**：极简白色主题，使用 CSS 变量（--bg, --panel, --accent 等）。
*   **动态粒子**：Canvas 实现的交互粒子背景，需保留滤镜模糊感。
*   **交互体验**：玻璃质感卡片、悬浮动效、动态渐变标题。

## 3. 核心功能点
*   **数据解耦**：所有项目信息需从 HTML 移至 JSON 数据源。
*   **分类功能 (Filtering)**：增加分类标签栏（如：全部、游戏、工具、2D/3D），支持点击实时过滤。
*   **排序功能 (Sorting)**：支持按日期最新、名称 A-Z 或预设权重排序。
*   **局域网共享**：确保升级后的开发服务器支持 `--host` 访问。

## 4. 数据库字段定义 (JSON Schema)
```json
{
  "projects": [
    {
      "id": "string",
      "title": "string",
      "author": "string",
      "description": "string",
      "coverImage": "string (relative path)",
      "projectUrl": "string",
      "tags": ["string"],
      "createdAt": "timestamp",
      "priority": "number"
    }
  ]
}
```

## 5. 执行步骤建议 (针对 Codex)
1.  **环境初始化**：在 `ai-club-hub` 同级创建 Vite+React 项目。
2.  **样式迁移**：将原 `index.html` 中的 CSS 抽离为全局样式。
3.  **粒子组件化**：将脚本中的 Canvas 逻辑封装为 React 组件。
4.  **数据迁移**：解析原 HTML 结构，将现有 12 个项目填入 `projects.json`。
5.  **逻辑实现**：使用 `useState` 和 `filter/sort` 方法实现列表的动态渲染。

---
*Generated by Antigravity for AI Club Hub Upgrade.*
